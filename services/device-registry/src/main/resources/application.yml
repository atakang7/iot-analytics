server:
  port: 8080

spring:
  application:
    name: device-registry
  datasource:
    url: jdbc:postgresql://${DB_HOST:localhost}:5432/${DB_NAME:iot}
    username: ${DB_USER:iot}
    password: ${DB_PASSWORD:iot}
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
