# Analytics Service

Python analytics service with clean, reusable core wrappers.

## Core Wrappers

- **MetricsServer**: Prometheus metrics HTTP server
- **MessageBroker**: RabbitMQ connection with configurable consumption strategies

## Usage

```bash
pip install -r requirements.txt
python -m analytics.main
```
